{
    "name": "totp",
    "scripts": {
        "start": "tsx --tsconfig tsconfig.json src",
        "test": "tsx --tsconfig tsconfig.json --test",
        "build": "tsc --project tsconfig.json",
        "bundle": "ncc build src/index.ts",
        "package": "pkg -t latest-win-x64 dist/index.js -o totp"
    },
    "dependencies": {
        "@scure/base": "^2.0.0"
    },
    "devDependencies": {
        "@types/node": "^25.0.3",
        "@vercel/ncc": "^0.38.4",
        "pkg": "^5.8.1",
        "tsup": "^8.5.1",
        "tsx": "^4.21.0",
        "typescript": "^5.9.3"
    }
}
